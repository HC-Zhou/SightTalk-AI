# SightTalk Frontend

React 前端模板，使用 npm 管理依赖和命令。

## 开发

```bash
npm install
npm run dev
```

## 常用命令

```bash
npm run lint
npm run test:run
npm run build
```

## 环境变量

复制 `.env.example` 为 `.env.local` 后按环境调整。
